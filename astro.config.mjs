// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://wanderingaround.co.uk',
  image: {
    layout: 'full-width',
    responsiveStyles: true,
  },
});
